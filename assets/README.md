# Assets

Replace these placeholder files with your real screenshots or GIFs when you deploy to GitHub Pages.

- `hero-demo.svg` or `hero-demo.gif`
- `feature-aiming.svg` or `feature-aiming.gif`
- `feature-calibration.svg` or `feature-calibration.gif`
- `feature-training.svg` or `feature-training.gif`

You can keep the same filenames so the page works without changing code.
